portfolio: "test-portfolio"
products:
  - name: "groovy-test-product"
    build:
      - echo "This is a build step!"
    cfn:
      template: "TestProduct.yaml"
      type: "yaml"
    deployProfile:
      - actions:
        - "s3:*"
        resources:
        - "*"
